{% extends 'base.html.twig' %}
{% block page_title %}Login{% endblock %}
{% block page_content %}
    <main>
        <form action="{{ path('login') }}" method="post" class="basic-form">
            <fieldset>
                <legend>Login</legend>
                <div>
                    <label for="email">Email</label>
                    <input id="email" type="email" name="_username" value="{{ lastEmail }}" required/>
                </div>
                <div>
                    <label for="password">Password</label>
                    <input id="password" type="password" name="_password" required/>
                </div>
                <button type="submit">Login</button>
                <div>
                    <a class="register-link" href="{{ path('register') }}">Register</a>
                </div>
            </fieldset>
            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
        </form>
    </main>
{% endblock %}